


<% if user_signed_in? && current_user.student? %>
    <%= render 'users/student' %>
  <% end %>


  <% if user_signed_in? && !current_user.student? %>
    <%= render 'users/admin' %>
    <% end %>
